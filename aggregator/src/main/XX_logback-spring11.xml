<configuration>
<!--    <include resource="org/springframework/boot/logging/logback/base.xml"/>-->
<!--    <springProperty scope="context" name="springAppName" source="spring.application.name"/>-->

    <!-- Appender to log to file in a JSON format -->
    <!--<appender name="logstash" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_FILE}.json</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${LOG_FILE}.json.%d{yyyy-MM-dd}.gz</fileNamePattern>
            <maxHistory>7</maxHistory>
        </rollingPolicy>
        <encoder class="net.logstash.logback.encoder.LoggingEventCompositeJsonEncoder">
            <providers>
                <timestamp>
                    <timeZone>UTC</timeZone>
                </timestamp>
                <pattern>
                    <pattern>
                        {
                        "level": "%level",
                        "service": "${springAppName:-}",
                        "trace": "%X{X-B3-TraceId:-}",
                        "span": "%X{X-B3-SpanId:-}",
                        "parent": "%X{X-B3-ParentSpanId:-}",
                        "thread": "%thread",
                        "class": "%logger{40}",
                        "message": "%message"
                        }
                        &lt;!&ndash; TODO: stacktrace, other fields&ndash;&gt;
                    </pattern>
                </pattern>
            </providers>
        </encoder>
    </appender>-->


    <!--<appender name="SPLUNK" class="ch.qos.logback.core.net.SyslogAppender">
        <syslogHost>splunk</syslogHost>
        <port>514</port>
        <suffixPattern>[%thread] %-5level %logger - %msg%n</suffixPattern>
    </appender>-->

    <!-- OR use HTTP appender -->
    <appender name="SPLUNK_HTTP" class="com.splunk.logging.HttpEventCollectorLogbackAppender">
        <url>http://splunk:8088</url>
<!--        url="http://localhost:8088/services/collector"-->
        <token>PUT-YOUR-HEC-TOKEN-HERE</token>
        <index>main</index>
        <source>springboot</source>
        <sourcetype>_json</sourcetype>
    </appender>

    <root level="INFO">
<!--        <appender-ref ref="logstash"/>-->
        <appender-ref ref="SPLUNK_HTTP"/>
    </root>
</configuration>
